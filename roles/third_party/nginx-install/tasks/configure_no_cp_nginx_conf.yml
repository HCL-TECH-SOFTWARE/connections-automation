- name:                      Create {{ __conf_d_location }}
  file:
    path:                    "{{ __conf_d_location }}"
    state:                   directory

- name:                     "Render {{ __no_cp_conf_template }} to {{ __no_cp_conf_location }}"
  template:
    src:                    "{{ __no_cp_conf_template }}"
    dest:                   "{{ __no_cp_conf_location }}"
    backup:                 yes

- name:                     Restart nginx service
  service:
    name:                   nginx
    state:                  restarted
    daemon_reload:          yes

- name:                     Wait for NGINX port 443 to be ready
  wait_for:
    port:                   443
    host:                   "{{ inventory_hostname }}"
    timeout:                60
    delay:                  5
