# Configure Global security > Federated repositories > Trusted authentication realms - inbound
AdminTask.addTrustedRealms('[-communicationType inbound -realmList {{ __trusted_realm_name }}]')
AdminConfig.save()
