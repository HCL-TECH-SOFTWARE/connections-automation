apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: "{{ __apisix_storage_class_name }}"
provisioner: kubernetes.io/no-provisioner
reclaimPolicy: "{{ __apisix_storage_class_reclaim_policy }}"
volumeBindingMode: Immediate
